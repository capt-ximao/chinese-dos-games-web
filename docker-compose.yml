services:
  app:
    image: ghcr.io/capt-ximao/chinese-dos-games-web:latest
    container_name: chinese-dos-games-web
    restart: unless-stopped
    network_mode: bridge
    ports:
      - "5000:5000"
    environment:
      - TZ=Asia/Shanghai
    volumes:
      - ./static/games:/app/static/games
